package cmd

import (
	"os"

	"github.com/spf13/cobra"
)

// rootCmd represents the base command when called without any subcommands
var rootCmd = &cobra.Command{
	Use:   "disvault",
	Short: "DisVault is a cli-tool for using discord as file storage",
	Long: `DisVault is a command-line tool designed to efficiently manage your files
by allowing you to upload, download, list, and organize them into groups.
With DisVault, you can easily handle large files by splitting them into chunks, 
and leverage groups to make file searching and management simpler.

It uses Discord as the file storage medium.`,
	// Uncomment the following line if your bare application
	// has an action associated with it:
	// Run: func(cmd *cobra.Command, args []string) { },
}

// Execute adds all child commands to the root command and sets flags appropriately.
// This is called by main.main(). It only needs to happen once to the rootCmd.
func Execute() {
	err := rootCmd.Execute()
	if err != nil {
		os.Exit(1)
	}
}

func init() {
	// Here you will define your flags and configuration settings.
	// Cobra supports persistent flags, which, if defined here,
	// will be global for your application.

	// rootCmd.PersistentFlags().StringVar(&cfgFile, "config", "", "config file (default is $HOME/.disvault.yaml)")
	rootCmd.Root().CompletionOptions.DisableDefaultCmd = true
	// Cobra also supports local flags, which will only run
	// when this action is called directly.
	// rootCmd.Flags().BoolP("toggle", "t", false, "Help message for toggle")
}
